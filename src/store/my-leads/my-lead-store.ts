import { create } from "zustand";



const useMyLeadStore = create()((set) => ({
    leadsData: [],
    page: 1,
    
    getLeads: async () => {
        try {

        } catch (error) {

        }
    }
}))